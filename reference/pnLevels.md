# Positive and Negative Levels'

A small modification to qgraph that allows cells to be colored as a
gradient of the level of the variable, on a scale from range `[-1:1]`

## Usage

``` r
pnLevels(levels)
```

## Arguments

- levels:

  ?the level of the factor? Need to review this some more

## Details

?Yeah, this function needs a little review to figure out how it is
working within `qgraph`
