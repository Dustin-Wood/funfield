% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPathXMY_ZLH.R
\name{plotPathXMY_ZLH}
\alias{plotPathXMY_ZLH}
\title{Two-panel low/high-Z field diagrams for a moderated X-M-Y model}
\usage{
plotPathXMY_ZLH(
  x,
  mediator = NULL,
  Z_levels = c(-1, 1),
  Z_label = "Z",
  panel_titles = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{\link{pathXMY}} or \code{\link{pathXMY_decompose}} return.}

\item{mediator, X_label, Y_label, Z_label, M_labels, X_shape, Y_shape, digits, scale_max, score_intensity_max, ...}{Passed through to \code{\link{plotPathXMY}}. See there for details.
In particular, \code{X_shape = "rtTri"} marks X as itself an imagined
action (speeding / overtime EXSJTs) and \code{Y_shape = "square"}
marks Y as a regular continuous outcome rather than an action
likelihood.}

\item{Z_levels}{Numeric length-2 vector of low and high Z values
(default \code{c(-1, 1)}, i.e. one SD below and above the mean for a
z-standardized between-person moderator).}

\item{panel_titles}{Optional length-2 character vector of panel
titles. If \code{NULL}, panel titles are auto-generated using
\code{Z_label} and the \code{Z_levels} values.}
}
\value{
A \code{patchwork} object combining two \code{ggplot} panels
  with a shared bottom legend. Auto-prints in interactive sessions
  and knitr chunks.
}
\description{
Returns a \pkg{patchwork} composition of two \code{\link{plotPathXMY}}
panels, one at a low value of \code{Z} and one at a high value. Each
panel renders the field with coefficients \emph{collapsed} at the
chosen \code{Z} value (\code{b1 + bZ * Z_value}), so the moderation
becomes visually concrete: edges thicken/thin, nodes shift color,
and the implied total effect on Y shifts between panels. The two
panels share a single color legend at the bottom.
}
\seealso{
\code{\link{plotPathXMY}}
}
