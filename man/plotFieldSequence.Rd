% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotFieldSequence.R
\name{plotFieldSequence}
\alias{plotFieldSequence}
\title{Plot a Sequence of Functional Field Time Steps}
\usage{
plotFieldSequence(
  sitlog,
  fprops,
  steps = NULL,
  titles = NULL,
  node_labels = NULL,
  label.cex = 0.65,
  ...
)
}
\arguments{
\item{sitlog}{Output of \code{\link{expOutcomesFF}}: a list with elements
\code{sit} (situation matrix), \code{dit} (disturbance matrix), and
\code{fit} (list of field states, one per time step).}

\item{fprops}{Output of \code{\link{prepField}}: a list containing
\code{matLayout}, \code{vnames}, \code{vshapes}, and \code{vsizes}.}

\item{steps}{Integer vector of time-step indices to plot. Defaults to all
valid steps (those with a non-\code{NULL} \code{$ft} element in
\code{sitlog$fit}).}

\item{titles}{Character vector of plot titles, one per element of
\code{steps}. Defaults to \code{"Step 1"}, \code{"Step 2"}, etc.}

\item{node_labels}{Optional named character vector mapping internal node
names to display labels (e.g.
\code{c("s1" = "Ready to make coffee", "make(s2).p" = "Set up coffeemaker")}).
When supplied, labels are drawn \emph{outside} each node (above action
nodes, below state/object nodes) rather than inside. Names not found in
\code{fprops$vnames} are silently ignored; nodes without an entry fall
back to their internal name.}

\item{label.cex}{Character expansion for external node labels. Default
\code{0.65}. Only used when \code{node_labels} is supplied.}

\item{...}{Additional arguments passed through to \code{qgraph::qgraph()}.}
}
\value{
The \code{sitlog} list, invisibly, so the call can be used in a
  pipe.
}
\description{
Visualize the time-step evolution of a functional field
simulation by plotting the field at each specified step. This function
replaces the manual pattern of incrementing \code{i} by hand and
re-running a \code{qgraph} call after \code{\link{expOutcomesFF}}.
}
\seealso{
\code{\link{expOutcomesFF}}, \code{\link{prepField}},
  \code{\link{pnLevels}}, \code{\link{fieldPolygons}}
}
